# Asahino Terra repository rules

## Motion-source and visual-QA gate

旭野テラのWebプレビューを変更するときは、静的なVRM表示だけで完了扱いにしない。

- AI人狼向けモーションは、参照専用リポジトリ
  `/Users/admin/Prj/yonagi-noa-vrm/public/motions/` にある補正済みVRMAを
  正式な再利用元とする。
- `raw-ardy/` の未補正版を公開プレイヤーへ接続しない。
- モーションファイルを取り込むだけでなく、旭野テラの実VRMでロード・再生・停止・
  切替をブラウザ上で確認する。
- 公開プレビューは、読込完了後に補正済み10モーションからランダム自動再生を開始する。
  同じモーションを連続選択せず、手動選択・停止・再開の操作性も維持する。
- 公開変更はmainへのpushだけで完了扱いにしない。対象コミットのGitHub Pagesジョブが
  `success`になり、公開URL上で`MODEL ONLINE`、`RANDOM AUTO`がON、実モーションが
  `PLAYING`であることをブラウザから同じ確認時点で検証する。ジョブ0件の起動前失敗や
  旧デプロイ表示は未完了として扱い、原因を解消してから完了を報告する。
- 新規または変更したモーション統合は、通常視点の25%・50%・75%と、
  手が顔・胴体・衣装へ近づく高リスク姿勢を正面および斜めから画像確認する。
- 手首、肘、肩、衣装への貫通、左右の手の重なり、不自然な関節、
  リセット後に姿勢が残る問題があれば完了にしない。
- 参照元`yonagi-noa-vrm`は読取専用で維持し、コピー後も前後の
  `git status --short`が空であることを確認する。

このルールは、「旭野テラ版でノアのモーションを活用する」というユーザー要件を、
今後の静的ビューア改修で再び落とさないための完了条件である。

## Live-preview performance gate

モーション、エフェクト、UI更新を追加・変更したときは、見た目と再生可否だけで
完了扱いにせず、公開プレビューの継続負荷も検証する。

- 独立したフルレートの`requestAnimationFrame`ループを安易に重ねない。
  装飾エフェクトは非操作時に停止または間引きされ、ランダム自動再生によって
  常時再点火されないことを確認する。
- Retina相当の`devicePixelRatio = 2`を含むデスクトップ表示で10秒以上計測し、
  FPS、p95フレーム時間、20ms超フレーム、long task、canvasの実バッファ寸法を記録する。
- 再生位置やARIA値などのDOM表示は毎フレーム無条件に書き換えず、
  値が変化した場合だけ、または視認に十分な低い頻度で同期する。
- ビューアが画面外または非表示のときは、VRM描画・モーション更新・装飾シミュレーションを
  一時停止できる構成を優先する。
- ユーザーからカクつきの指摘があった場合は、公開ページ自体の負荷とMacの
  CPU・メモリ圧・他プロセス負荷を別々に測り、端末負荷だけを原因にしない。

このルールは、ランダムモーションの開始イベントがフルスクリーン流体エフェクトを
繰り返し再点火し、視覚上は正常でも描画余力を失う問題を再発させないための完了条件である。
